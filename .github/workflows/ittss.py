minStudents = 15
maxStudents = 40

currentNum = int(input("Enter the number of current students: "))

if currentNum >= minStudents and currentNum <= maxStudents:
            print("The number is Ok!")
